import { BoxProps } from '@chakra-ui/react';

const imageContainerStyle: BoxProps = {
  position: 'relative',
  height: 'full',
  width: 'full',
  overflow: 'hidden',
};

export default imageContainerStyle;
